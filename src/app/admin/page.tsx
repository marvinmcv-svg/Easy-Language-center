import * as React from "react";
import { AdminPortal } from "@/components/admin";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "ELC Admin — Easy Learning Center",
};

export default function AdminPage() {
  return <AdminPortal />;
}
